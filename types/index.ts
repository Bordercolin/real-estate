/**
 * Shared TypeScript types and interfaces.
 */

export interface Property {
  id: string;
  title: string;
  address: string;
  price: number;
  beds: number;
  baths: number;
  imageUrl: string;
  slug: string;
}
